{
  "name": "js-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter jsdesign-ui watch",
    "dev:ui": "pnpm --filter jsdesign-ui dev",
    "build": "pnpm --filter jsdesign-ui build",
    "typecheck": "pnpm -r run typecheck",
    "mcp": "pnpm --filter jsdesign-mcp-bridge start:dev"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "concurrently": "10.0.4",
    "typescript": "^7.0.2"
  }
}
